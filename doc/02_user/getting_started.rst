Getting Started
===============

Generate the default RTL:

.. code-block:: console

   make verilog

The default target emits an AXI-facing ``CL1Top`` with ICache and DCache
enabled.
Generated RTL is written under ``vsrc``.
